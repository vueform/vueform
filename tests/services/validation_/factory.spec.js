import { createForm } from './../../src/utils/testHelpers'

describe('Validation Service Factory', () => {
  it('should split rules by pipe', (done) => {
    done()
  })

  it('should add rules from array', (done) => {
    done()
  })

  it('should handle rule params', (done) => {
    done()
  })

  it('should ignore sometimes', (done) => {
    done()
  })
})